<h1>Stocks</h1>

<table>
  <tr>
    <th>Symbol</th>
    <th>QTY</th>
    <th></th>
  </tr>

  <% @stocks.each do |stock| %>
    <tr>
      <td><%= stock.symbol %></td>
      <td><%= stock.quantity %></td>
      <td><%= link_to 'More Info', stock_path(stock) %></td>
    </tr>
  <% end %>
</table>
